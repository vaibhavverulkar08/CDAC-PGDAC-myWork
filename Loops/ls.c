#include<stdio.h>
int main(){
char str[20];
int i;
printf("\nEnter any string:\n");
scanf("%s",str);
for (i = 0; str[i]!=0; i++)
{
    
}
printf("\nLength of the string is:%d",i);
return 0;
}
//Q4. write a program in which declare all data type like integer , double , float, long int  and p
// print value in specicified formate.
#include <stdio.h>
int main()
{

    int number = 20;
    double d = 8686.997;
    float pi = 3.14;
    long int employeeId;
    printf("value for integer is for ex number:%d\n", number);
    // printf("sum of enterd two number is:",sum);
    printf("value for doube is for ex student roll number:%.2lf\n", d);
    printf("value for float is for example pi value:%.2f\n", pi);
    printf("value for longint is for example employee id :%.2f\n", pi);

    return 0;
}
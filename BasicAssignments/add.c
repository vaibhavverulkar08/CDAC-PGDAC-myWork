// 2.1 Write a program to find the addition of two numbers.
//1. WAP to calculate the sum of two variables.

#include <stdio.h>
    int main()
    {
        int a = 10, b = 20, c;
        c = a + b;
        printf("\n Additon of two number is:%d", c);

        //or // Taking Input  from user.
        int e,d;
        scanf("%d%d", &e, &d);
        printf("\n Additon of two number is:%d", e+d);

return 0;

    }